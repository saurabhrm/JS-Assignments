// 15. Calculate the slope, x-intercept and y-intercept of y = 2x -2

    // y= mx-b

    // slope
    let m = 2;
    console.log(`The Slope (m) is ${m}`);

    // y-Intercept 
    let y1= 0;
    let y2 = -2;


    console.log(`the y-intercept is (${y1} , ${y2})`);

    // x-Intercept ==> formula - y=mx+b

    let xIntercept= ((y1-y2)/m);

    console.log(`the x-intercept is (${xIntercept} , ${0})`);
